
[x] show how much money is left = budget - current weekly status
[x] deploy to Railway automatically ONLY if the current env is production (set a local var?)
[ ] make sure that the week that is "not full" - i.e. start of the month -> Oct 1st is Wed so the week is -> Sun


[ ] add an option to reduce the next weekly budget based on the amount of money's left



[ ] if the budget was already inserted - update it - don't insert the new one
[ ] if the category cannot be recognized - DO NOT discard the first word - put it into the description.
[x] show the current status with a graph - https://quickchart.io/

This is more important for accuracy right now ->

[ ] make sure that the week that is "not full" - i.e. start of the month -> Oct 1st is Wed so the week is -> Sun

[ ] create a proper chart for categories

[ ] if the category cannot be recognized - DO NOT discard the first word - put it into the description.
[ ] show the current status with a graph - https://quickchart.io/


    [x] fixate the budget for the week at the start of the week
        [ ] run cron job
        [ ] monthly budget / amount of weeks -> write into the database

[ ] allow to delete / change the transaction after it was already posted via a button
[ ] pin chat message
    at the start of the week
    https://chatgpt.com/c/68d80a2f-613c-8322-b3e2-eef0efb75cfd

   https://crontab.guru/#5_4_25_*_*
    - notify the chat as to what's the budget fo reach category left for the week (and this budget sticks until next week)

[ ] Add the ability to set budget for the month for a specific category.
[ ] connect an API for converting currencies

[ ] make sure that the week that is "not full" - i.e. start of the month -> Oct 1st is Wed so the week is -> Sun

- determine budget per day.

if Sunday of current week month is different from current Month. Determine the amount of days left in this month and
and multiply daily budget by that amount of days.

else if Monday month is different from current Month. Determine when the current month starts and get that amount of days
from current day

else  simply multiply by 7 and return because we're in a full week within a month




#name: Weekly Telegram ping
#on:
#  schedule:
#    # Every Monday 09:00 Bangkok time (UTC+7 = 02:00 UTC)
#    - cron: "*/1 * * * *"
#jobs:
#  send:
#    runs-on: ubuntu-latest
#    steps:
#      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
#      - name: Send message
#        run: |
#          curl -s -X POST "https://api.telegram.org/bot${{ secrets.BOT_TOKEN }}/sendMessage" \
#            -d chat_id="${{ secrets.CHAT_ID }}" \
#            -d text="5 minute ping не обращайтэ внимание🎉" \
#            -d parse_mode="Markdown"
